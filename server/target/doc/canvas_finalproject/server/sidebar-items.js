initSidebarItems({"struct":[["CanvasServer","A struct that contains implementation for behavior associated with the entire server, like creating `ClientHandler` events when a client tries to establish a connection initially."],["ClientHandler","A struct that contains the implementation for behavior associated with a single client, like when responding to a single client's requests. For now, all client handlers are created equal because we treat all our clients equally, and so the fields in these objects are all the same. However, this struct gives room for expansion."]]});